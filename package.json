{
  "name": "mymailer",
  "version": "1.0.0",
  "description": "the mails sender",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "Swalest",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^10.2.8",
    "@types/node": "^20.9.0",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.7",
    "rimraf": "^5.0.5",
    "rxjs": "^7.8.1",
    "typescript": "^5.2.2"
  }
}
